//! initialize / initialized / shutdown
//!
//! initialize 握手由 connection.rs 的 `Connection::initialize` 处理，
//! 本模块保留供后续扩展（如读取 root_uri 初始化项目索引）。

// MVP 阶段 initialize 握手已在 connection.rs 完成，此处为预留扩展点。
