[package]
name = "rust-rml-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "RML 框架核心 trait 与基础类型定义"

[dependencies]
gpui = { workspace = true }
gpui-component = { workspace = true, optional = true }
serde_json = "1"
ctor = "0.2"
url = "2"
flume = "0.12"

[features]
gpui-component = ["dep:gpui-component"]
