[package]
name = "tree-sitter-rml"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "RML grammar for tree-sitter"

build = "build.rs"

[dependencies]
tree-sitter = "0.26"
tree-sitter-language = "0.1"

[build-dependencies]
cc = "1"

[lib]
path = "lib.rs"
