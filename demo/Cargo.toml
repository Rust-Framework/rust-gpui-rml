[package]
name = "rust-rml-demo"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "RML 计数器 demo —— 验证 .rml + .rml.rs + build.rs 三件套闭环"

[dependencies]
rust-rml-engine = { workspace = true }
rust-rml-app = { workspace = true }
rust-rml-core = { workspace = true }
gpui = { workspace = true }

[build-dependencies]
rust-rml-engine = { workspace = true }
