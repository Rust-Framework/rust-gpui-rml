<component>
    <div display="flex" flex-direction="column" class="case-pane doc-pane">
        <h2>{t("case.popover.title")}</h2>
        <p>Popover 是 RML 的浮动气泡容器组件，标签为 PascalCase <code>Popover</code>。子节点通过 <code>slot="trigger"</code> 路由到 <code>.trigger()</code>，其余子节点作为 content 注入 <code>.child()</code>。<code>anchor</code> 属性控制气泡定位锚点，<code>default-open</code> 控制初始展开状态。</p>

        <Card title="基础用法">
            <p>点击按钮展开气泡内容：</p>
            <Popover>
                <Button slot="trigger" label="点击展开" />
                <div display="flex" flex-direction="column" gap="8px" padding="12px">
                    <p>这是气泡内容。</p>
                    <p>可以放置任意元素。</p>
                </div>
            </Popover>
        </Card>

        <Card title="锚点定位 anchor">
            <p><code>anchor</code> 属性控制气泡相对于 trigger 的定位：</p>
            <div display="flex" flex-direction="row" gap="16px">
                <Popover anchor="bottom-left">
                    <Button slot="trigger" label="bottom-left" />
                    <div padding="8px">左下角锚点</div>
                </Popover>
                <Popover anchor="bottom-center">
                    <Button slot="trigger" label="bottom-center" />
                    <div padding="8px">中下角锚点</div>
                </Popover>
                <Popover anchor="bottom-right">
                    <Button slot="trigger" label="bottom-right" />
                    <div padding="8px">右下角锚点</div>
                </Popover>
            </div>
        </Card>

        <Card title="默认展开 default-open">
            <p><code>default-open="true"</code> 让气泡初始处于展开状态：</p>
            <Popover default-open="true">
                <Button slot="trigger" label="已展开" />
                <div padding="8px">初始展开的气泡内容</div>
            </Popover>
        </Card>

        <Card title="示例代码">
            <p>点击下方 Tab 切换 <code>.rml</code> 声明文件与 <code>.rml.rs</code> 后端文件：</p>
            <TabBar selected-index={code_tab} on-click={on_code_tab_change}>
                <Tab label=".rml">
                    <CodeEditor ref="rml_editor" value={rml_sample} language="rml" />
                </Tab>
                <Tab label=".rml.rs">
                    <CodeEditor ref="rust_editor" value={rust_sample} language="rust" />
                </Tab>
            </TabBar>
        </Card>

        <Card title="API">
            <h3>Popover</h3>
            <Table columns={api_columns} rows={api_rows} bordered="" stripe="" />
        </Card>
    </div>
</component>
