<component>
    <div v-flex="" class="case-pane doc-pane">
        <h2>{t("case.icon.title")}</h2>

        <Card title="组件说明">
            <p>Icon 组件渲染矢量图标。构造器接受 IconName 枚举或 path 字符串：name="Settings" 生成 Icon::new(IconName::Settings)，path="icons/foo.svg" 生成 Icon::empty().path(...)。RenderOnce 无 ElementId，由专属 compiler/icon 模块处理 name/path 属性提取。</p>
        </Card>

        <Card title="基础用法">
            <p>静态 IconName：</p>
            <div h-flex="" gap-4="">
                <Icon name="Settings" />
                <Icon name="Bell" />
                <Icon name="User" />
            </div>
            <p>不同尺寸（size 走通用 Sizable setter）：</p>
            <div h-flex="" gap-4="">
                <Icon name="Settings" size="xsmall" />
                <Icon name="Settings" size="small" />
                <Icon name="Settings" size="medium" />
                <Icon name="Settings" size="large" />
            </div>
        </Card>

        <Card title="动态切换">
            <p>当前图标：{current_icon_name}（icon_index = {icon_index}）</p>
            <div h-flex="" gap-4="">
                <Icon name={current_icon} size="large" />
                <Button label="切换图标" on-click={on_rotate_icon} />
            </div>
            <p>注：name=&#123;current_icon&#125; 绑定到 computed 方法，返回 rml_ui::IconName 枚举值。</p>
        </Card>

        <Card title="自定义路径">
            <p>path 属性指定自定义图标路径（相对 assets 目录）：</p>
            <Icon path="icons/custom.svg" size="medium" />
        </Card>

        <Card title="API">
            <Table columns={api_columns} rows={api_rows} bordered="" stripe="" />
        </Card>
    </div>
</component>
