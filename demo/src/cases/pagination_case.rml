<component>
    <CaseDocPage
        title={t("case.pagination.title")}
        description="Pagination 是 RML 的分页组件，标签为 PascalCase Pagination。支持 current-page、total-pages、visible-pages、compact 等属性（kebab-case），on-click 事件签名为 Fn(&amp;usize, ...)（页码）。"
        code-rml={rml_sample}
        code-rust={rust_sample}>
        <template slot="demo">
            <div class="demo-section">
                <h3>基础用法</h3>
                <p><code>current-page</code> 设置当前页（1-based），<code>total-pages</code> 设置总页数，<code>on-click</code> 处理页码切换：</p>
                <Pagination current-page={current_page} total-pages={total_pages} on-click={on_page_change} />
                <p>当前页：{current_page} / {total_pages}</p>
            </div>

            <div class="demo-section">
                <h3>visible-pages 控制显示页码数</h3>
                <p><code>visible-pages</code> 设置最大可见页码数（默认 5）：</p>
                <Pagination current-page={current_page} total-pages="20" visible-pages="7" on-click={on_page_change} />
                <Pagination current-page={current_page} total-pages="20" visible-pages="10" on-click={on_page_change} />
            </div>

            <div class="demo-section">
                <h3>compact 紧凑模式</h3>
                <p><code>compact=""</code> 仅显示前后箭头按钮：</p>
                <Pagination current-page={current_page} total-pages={total_pages} compact="" on-click={on_page_change} />
            </div>

            <div class="demo-section">
                <h3>静态属性</h3>
                <p>使用静态数值属性（无绑定）：</p>
                <Pagination current-page="3" total-pages="10" on-click={on_page_change} />
                <p>注：静态属性的 Pagination 不会自动更新页码，需要绑定字段做状态管理。</p>
            </div>

            <div class="demo-section">
                <h3>完整分页示例</h3>
                <p>模拟分页场景（共 50 条数据，每页 5 条 = 10 页）：</p>
                <div display="flex" flex-direction="column" gap="8px">
                    <p>第 {current_page} 页 / 共 10 页</p>
                    <Pagination current-page={current_page} total-pages="10" on-click={on_page_change} />
                </div>
            </div>
        </template>

        <template slot="api">
            <h3>Pagination</h3>
            <Table columns={api_columns} rows={api_rows} bordered="" stripe="" />
            <p>另：<code>on-click</code> 事件签名为 <code>Fn(&amp;usize, &amp;mut Window, &amp;mut App)</code>（页码），不是 ClickEvent。</p>
        </template>
    </CaseDocPage>
</component>
