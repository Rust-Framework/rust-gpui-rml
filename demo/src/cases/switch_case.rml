<component>
    <div v-flex="" class="case-pane doc-pane">
        <h2>{t("case.switch.title")}</h2>
        <p>Switch 是 RML 的开关组件，标签为 PascalCase <code>Switch</code>。Stateless 组件，通过 <code>checked</code> 属性绑定开关状态（映射到 <code>.checked()</code> 方法）。</p>

        <Card title="基础用法">
            <p><code>checked</code> 属性绑定布尔字段，<code>on-click</code> 回调签名 <code>(&amp;bool, &amp;mut Context)</code>。</p>
            <p>{status_text}</p>
            <div h-flex="" gap-4="">
                <Switch label="自动保存" checked={is_on} on-click={on_toggle} />
                <Button label="切换开关" on-click={on_toggle_button} />
            </div>
        </Card>

        <Card title="禁用状态 disabled">
            <p><code>disabled</code> 属性禁用开关交互。</p>
            <div h-flex="" gap-4="">
                <Switch checked={is_disabled} disabled={is_disabled} />
                <Button label="切换禁用" on-click={on_toggle_disabled} />
            </div>
        </Card>

        <Card title="示例代码">
            <p>点击下方 Tab 切换 <code>.rml</code> 声明文件与 <code>.rml.rs</code> 后端文件：</p>
            <TabBar selected-index={code_tab} on-click={on_code_tab_change}>
                <Tab label=".rml" />
                <Tab label=".rml.rs" />
            </TabBar>
            <div if={code_tab == 0}>
                <CodeEditor ref="rml_editor" value={rml_sample} language="rust" />
            </div>
            <div if={code_tab == 1}>
                <CodeEditor ref="rust_editor" value={rust_sample} language="rust" />
            </div>
        </Card>

        <Card title="API">
            <h3>Switch</h3>
            <Table columns={api_columns} rows={api_rows} bordered="" stripe="" />
        </Card>
    </div>
</component>
