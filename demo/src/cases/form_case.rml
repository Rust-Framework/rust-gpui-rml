<component>
    <CaseDocPage
        title={t("case.form.title")}
        description="Form 是声明式表单容器组件。通过独立布尔属性 horizontal/vertical 选择布局方向（默认 vertical），label-width 设置标签宽度，columns 设置列数。Field 是表单字段容器，支持 label、description、required、visible、col-span 等属性，可包含任意表单控件。"
        code-rml={rml_sample}
        code-rust={rust_sample}>
        <template slot="demo">
            <div class="demo-section">
                <h3>垂直布局（默认）</h3>
                <p>Form 默认垂直布局，标签位于内容上方。每个 Field 通过 label 属性设置标签文本。</p>
                <Form class="form-demo">
                    <Field label="用户名">
                        <Input ref="username_input" placeholder="请输入用户名" />
                    </Field>
                    <Field label="邮箱">
                        <Input ref="email_input" placeholder="请输入邮箱" />
                    </Field>
                    <Field label="备注">
                        <Input ref="remark_input" placeholder="请输入备注" />
                    </Field>
                </Form>
            </div>

            <div class="demo-section">
                <h3>水平布局 horizontal</h3>
                <p>通过 horizontal 属性切换为水平布局，标签位于内容左侧。label-width 控制标签宽度（默认 140px）。</p>
                <Form horizontal label-width="100" class="form-demo">
                    <Field label="姓名">
                        <Input ref="name_input" placeholder="请输入姓名" />
                    </Field>
                    <Field label="年龄">
                        <Input ref="age_input" placeholder="请输入年龄" />
                    </Field>
                    <Field label="地址">
                        <Input ref="address_input" placeholder="请输入地址" />
                    </Field>
                </Form>
            </div>

            <div class="demo-section">
                <h3>必填与描述</h3>
                <p>Field 支持 required 布尔属性（显示 * 标记）和 description 属性（显示帮助文本）。</p>
                <Form horizontal label-width="100" class="form-demo">
                    <Field label="密码" required description="至少 8 位字符">
                        <Input ref="password_input" placeholder="请输入密码" masked />
                    </Field>
                    <Field label="确认密码" required description="再次输入密码">
                        <Input ref="confirm_input" placeholder="再次输入密码" masked />
                    </Field>
                </Form>
            </div>

            <div class="demo-section">
                <h3>多列布局 columns</h3>
                <p>通过 columns 属性设置列数，Field 通过 col-span 控制跨列。适用于紧凑的表单布局。</p>
                <Form horizontal label-width="80" columns="2" class="form-demo">
                    <Field label="名">
                        <Input ref="first_name_input" placeholder="名" />
                    </Field>
                    <Field label="姓">
                        <Input ref="last_name_input" placeholder="姓" />
                    </Field>
                    <Field label="城市">
                        <Input ref="city_input" placeholder="城市" />
                    </Field>
                    <Field label="邮编">
                        <Input ref="zipcode_input" placeholder="邮编" />
                    </Field>
                    <Field label="地址" col-span="2">
                        <Input ref="full_address_input" placeholder="请输入地址" />
                    </Field>
                </Form>
            </div>

            <div class="demo-section">
                <h3>表单控件组合</h3>
                <p>Field 可包含任意表单控件：Switch、Checkbox、Button 等。下方演示不同控件类型的表单字段。</p>
                <Form horizontal label-width="100" class="form-demo">
                    <Field label="启用">
                        <Switch />
                    </Field>
                    <Field label="记住我">
                        <Checkbox label="记住登录状态" />
                    </Field>
                    <Field label="操作">
                        <Button label="提交" primary />
                    </Field>
                </Form>
            </div>
        </template>

        <template slot="api">
            <h3>Form</h3>
            <Table columns={form_api_columns} rows={form_api_rows} bordered="" stripe="" />
            <h3>Field</h3>
            <Table columns={field_api_columns} rows={field_api_rows} bordered="" stripe="" />
            <p>注意：Form 子节点必须为 Field 元素。</p>
            <p>另：variant 布尔属性（horizontal/vertical）为互斥布局选择，不设置时默认 vertical。</p>
        </template>
    </CaseDocPage>
</component>
