<component>
    <div v-flex="" class="case-pane doc-pane">
        <h2>{t("case.input.title")}</h2>

        <Card title="组件说明">
            <p>Input 是 RML 的文本输入组件。Stateful 模式，通过 ref 指令惰性创建 InputState Entity，由 __rml_populate_refs 注入到对应的 ElementRef 字段。</p>
            <p>placeholder 与 default_value 需在 InputState builder 上设置，disabled 通过 Input 组件 RML 属性设置。</p>
        </Card>

        <Card title="基础用法">
            <p>通过 ref 指令绑定到 ElementRef 字段，InputState Entity 在首次渲染时自动创建。</p>
            <Input ref="input_state" />
        </Card>

        <Card title="API">
            <Table columns={api_columns} rows={api_rows} bordered="" stripe="" />
        </Card>
    </div>
</component>
