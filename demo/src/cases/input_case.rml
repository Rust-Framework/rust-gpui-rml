<component>
    <CaseDocPage
        title={t("case.input.title")}
        description="Input 是 RML 的文本输入组件。支持 ref 引用、value 双向绑定、placeholder、default-value、masked 密码模式，以及 disabled、size、selected 等属性。小写 input 标签用法相同，详见 two-way-case 演示。"
        code-rml={rml_sample}
        code-rust={rust_sample}>
        <template slot="demo">
            <div class="demo-section">
                <h3>1. 基础用法 + ref 指令</h3>
                <p>通过 ref 指令绑定到 ViewModel 同名字段，便于命令式访问（如聚焦、取值）。</p>
                <Input ref="basic_input" />
            </div>

            <div class="demo-section">
                <h3>2. placeholder 占位文本</h3>
                <p>placeholder 属性直接在 RML 中设置占位文本，无需手动创建 InputState。ref 路径和 value 路径均支持。</p>
                <Input ref="placeholder_input" placeholder="请输入用户名" />
            </div>

            <div class="demo-section">
                <h3>3. default-value + masked 密码输入</h3>
                <p>default-value 设置默认值，masked 启用密码遮罩模式。两者均为 InputState builder 参数，在构造时注入。</p>
                <Input ref="masked_input" default-value="secret123" masked placeholder="请输入密码" />
            </div>

            <div class="demo-section">
                <h3>4. disabled 禁用</h3>
                <p>disabled 是 Input 组件属性，通过 RML 绑定 disabled={is_disabled} 切换。点击按钮切换禁用状态。</p>
                <p>当前状态：{disabled_status_text}</p>
                <div class="button-row">
                    <Button label="切换禁用" on-click={on_toggle_disabled} />
                    <Input ref="disabled_input" disabled={is_disabled} placeholder="禁用态输入" />
                </div>
            </div>

            <div class="demo-section">
                <h3>5. 尺寸 size</h3>
                <p>size 属性设置尺寸：xsmall / small / medium / large。点击按钮循环切换尺寸。size 绑定需返回尺寸枚举值（size_value computed）。</p>
                <p>当前尺寸：{size_label}</p>
                <div class="button-row">
                    <Button label="切换尺寸" on-click={on_cycle_size} />
                    <Input ref="sized_input" size={size_value} placeholder="尺寸变体" />
                </div>
            </div>

            <div class="demo-section">
                <h3>6. selected 选中态</h3>
                <p>selected 属性走通用 Selectable setter，切换 Input 的选中状态（视觉高亮）。点击按钮切换选中状态。</p>
                <p>当前状态：{selected_status_text}</p>
                <div class="button-row">
                    <Button label="切换选中" on-click={on_toggle_selected} />
                    <Input ref="selected_input" selected={is_selected} placeholder="选中态输入" />
                </div>
            </div>

            <div class="demo-section">
                <h3>7. 多 Input 组合（表单布局）</h3>
                <p>多个 ref 字段在 flex 布局中并排，演示真实表单场景。每个 Input 独立持有引用，互不干扰。</p>
                <div display="flex" flex-direction="column" gap="8px">
                    <Input ref="form_name_input" placeholder="姓名" />
                    <Input ref="form_email_input" placeholder="邮箱" />
                </div>
            </div>
        </template>

        <template slot="api">
            <h3>Input</h3>
            <Table columns={api_columns} rows={api_rows} bordered="" stripe="" />
            <p>另：size 属性为通用尺寸属性，disabled/selected 等通用属性对所有组件生效。PascalCase Input 与小写 input 是两种不同路径：前者通过 ref 指令引用状态并订阅事件，后者通过 value 双向绑定自动同步。小写 input 的 value 双向绑定演示见 two-way-case。</p>
        </template>
    </CaseDocPage>
</component>
