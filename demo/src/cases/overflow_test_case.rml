<component>
    <CaseDocPage
        title="Overflow Test"
        description="验证 CSS overflow-x / overflow-y 映射与滚动容器行为。overflow-x-auto / overflow-y-auto 布尔标志等同 overflow-x: auto / overflow-y: auto。"
        code-rml={rml_sample}
        code-rust={rust_sample}>
        <template slot="demo">
            <Card title="垂直滚动 overflow-y-auto">
                <p>50 个条目，固定高度 200px，验证 overflow-y 映射：</p>
                <div class="scroll-vertical" overflow-y-auto="" style="height: 200px; border: 1px solid #e0e0e0; padding: 8px;">
                    <p each={item in items}>{item}</p>
                </div>
            </Card>
        </template>
        <template slot="api">
            <Table columns={api_columns} rows={api_rows} bordered="" stripe="" />
        </template>
    </CaseDocPage>
</component>
