<component>
    <CaseDocPage
        title={t("case.avatar.title")}
        description="Avatar 是 RML 的头像组件，标签为 PascalCase Avatar 或小写 avatar（均合法）。无需 ElementId。支持三种内容模式（src 图片 / name 首字母 / placeholder 占位图标），优先级 src &gt; name &gt; placeholder。四种尺寸（xsmall/small/medium/large），支持 on-click 事件。AvatarGroup 容器组合多个 Avatar，支持 limit 限制与 ellipsis 折叠。"
        code-rml={rml_sample}
        code-rust={rust_sample}>
        <template slot="demo">
            <div class="demo-section">
                <h3>内容模式：src 图片源</h3>
                <p>src 属性指定图片 URL，组件内部加载图片并裁剪为圆形。加载失败时自动回退到 name 或 placeholder。下方演示三种尺寸的图片头像。</p>
                <div class="avatar-row">
                    <Avatar src="https://avatars.githubusercontent.com/u/5518?v=4" size="large" />
                    <Avatar src="https://avatars.githubusercontent.com/u/28998859?v=4" />
                    <Avatar src="https://avatars.githubusercontent.com/u/20092316?v=4" size="small" />
                </div>
            </div>

            <div class="demo-section">
                <h3>内容模式：name 首字母</h3>
                <p>name 属性提取首字母显示（如 Jason Lee → JL）。无 src 时使用，常用于用户名头像。</p>
                <div class="avatar-row">
                    <Avatar name="Jason Lee" size="large" />
                    <Avatar name="Floyd Wang" />
                    <Avatar name="Alice" size="small" />
                </div>
            </div>

            <div class="demo-section">
                <h3>内容模式：placeholder 占位图标</h3>
                <p>无 src/name 时显示 IconName 图标作为占位符。值为 IconName 枚举名（PascalCase，如 CircleUser/Building2/Bell）。常用于匿名用户或组织头像。</p>
                <div class="avatar-row">
                    <Avatar size="large" placeholder="CircleUser" />
                    <Avatar placeholder="Building2" />
                    <Avatar size="small" placeholder="Bell" />
                </div>
            </div>

            <div class="demo-section">
                <h3>尺寸 size</h3>
                <p>size 属性控制头像大小：xsmall / small / medium / large，走通用 Sizable trait。</p>
                <div class="avatar-row">
                    <Avatar name="XS" size="xsmall" />
                    <Avatar name="S" size="small" />
                    <Avatar name="M" size="medium" />
                    <Avatar name="L" size="large" />
                </div>
            </div>

            <div class="demo-section">
                <h3>动态绑定（value + computed + if）</h3>
                <p>input 通过 value={name} 双向绑定姓名字段，点击按钮循环切换尺寸，观察头像实时变化。此案例演示 RML 数据驱动的核心能力：value 双向绑定 + computed 计算属性 + if 条件渲染。</p>
                <input value={name} placeholder="输入姓名" />
                <p>当前姓名：{name} | 当前尺寸：{size_label}</p>
                <div class="avatar-row">
                    <div if={size_index % 3 == 0}>
                        <Avatar name={name} size="small" />
                    </div>
                    <div if={size_index % 3 == 1}>
                        <Avatar name={name} size="medium" />
                    </div>
                    <div if={size_index % 3 == 2}>
                        <Avatar name={name} size="large" />
                    </div>
                </div>
                <Button label="切换尺寸" primary on-click={on_cycle_size} />
            </div>

            <div class="demo-section">
                <h3>头像分组 AvatarGroup</h3>
                <p>AvatarGroup 容器组合多个 Avatar，limit 属性限制显示数量，ellipsis 属性开启溢出折叠（显示 +N 提示）。详见 avatar_group_case 案例。</p>
                <AvatarGroup limit="3" ellipsis="">
                    <Avatar src="https://avatars.githubusercontent.com/u/5518?v=4" />
                    <Avatar src="https://avatars.githubusercontent.com/u/28998859?v=4" />
                    <Avatar src="https://avatars.githubusercontent.com/u/20092316?v=4" />
                    <Avatar src="https://avatars.githubusercontent.com/u/22312482?v=4" />
                    <Avatar name="John Doe" />
                </AvatarGroup>
            </div>
        </template>

        <template slot="api">
            <h3>Avatar</h3>
            <Table columns={avatar_api_columns} rows={avatar_api_rows} bordered="" stripe="" />
            <h3>AvatarGroup</h3>
            <Table columns={group_api_columns} rows={group_api_rows} bordered="" stripe="" />
            <p>另：size 属性来自 Sizable trait，disabled 等通用属性对所有组件生效。</p>
            <p>另：avatar 与 Avatar 标签等价。</p>
        </template>
    </CaseDocPage>
</component>
