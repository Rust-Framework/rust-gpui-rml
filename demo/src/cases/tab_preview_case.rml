<component>
    <div display="flex" flex-direction="column" class="case-pane doc-pane">
        <h2>{t("case.tab_preview.title")}</h2>

        <p>VSCode 风格的 tab 体验：关闭按钮 tooltip（i18n）、右键菜单（Close/Close All/Close Others）、预览模式（italic 标题）、双击 promote。框架内置三个标准菜单项，文本走 i18n（rml.tab.close / rml.tab.close_all / rml.tab.close_others）。业务通过 on-close / on-close-all / on-close-others 回调驱动数据层变更。双击 tab 触发 on-promote 回调（250ms 时间窗口）。</p>

        <Card title="演示效果">
            <p>{status_text}</p>

            <div class="demo-section">
                <div display="flex" flex-direction="row" gap="8px">
                    <Button label="重置" on-click={on_reset} />
                </div>
                <p>提示：右键 tab 弹出菜单；双击 preview tab（斜体标题）触发 promote；hover 关闭按钮显示 tooltip。</p>
                <Tabs
                    selected-index={selected_index}
                    on-click={on_tab_select}
                    on-close={on_tab_close}
                    on-close-all={on_tab_close_all}
                    on-close-others={on_tab_close_others}
                    on-promote={on_tab_promote}>
                    <Tab each={tab in tabs}
                        label={tab.title}
                        closable={tab.closable}
                        preview={tab.preview} />
                </Tabs>
            </div>
        </Card>

        <Card title="代码示例">
            <Tabs selected-index={code_tab} on-click={on_code_tab_change}>
                <Tab label=".rml">
                    <CodeEditor ref="rml_editor" value={rml_sample} language="rml" />
                </Tab>
                <Tab label=".rml.rs">
                    <CodeEditor ref="rust_editor" value={rust_sample} language="rust" />
                </Tab>
            </Tabs>
        </Card>

        <h3>Tabs / Tab</h3>
        <Card title="API">
            <h3>Tabs</h3>
            <Table columns={tabs_api_columns} rows={tabs_api_rows} bordered="" stripe="" />
            <h3>Tab</h3>
            <Table columns={tab_api_columns} rows={tab_api_rows} bordered="" stripe="" />
        </Card>
    </div>
</component>
