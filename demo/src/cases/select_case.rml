<component>
    <CaseDocPage
        title={t("case.select.title")}
        description="Select 是 RML 的下拉选择器组件，标签为 PascalCase Select 或 kebab-case select。StatefulWithDelegate 模式，构造器 SelectState::new(delegate, None, window, cx) 创建状态，Select::new(&state) 创建视图。delegate 为 SearchableVec<SharedString>，通过 items={field} 绑定提供。支持 placeholder、cleanable、appearance、menu_width、menu_max_h、size 等属性。on_change 通过 cx.subscribe 订阅 SelectEvent::Confirm(Option<SharedString>) 触发，用户方法直接接收 Option<SharedString> 载荷。"
        code-rml={rml_sample}
        code-rust={rust_sample}>
        <template slot="demo">
            <div class="demo-section">
                <h3>1. 基础用法 + on_change 事件</h3>
                <p>Select 通过 ref 指令绑定到 ElementRef SelectState 字段，items 绑定提供 SearchableVec 委托数据。选择后触发 SelectEvent::Confirm，参数为 Option&lt;SharedString&gt;。</p>
                <p>选中值：{selected_value}</p>
                <Select ref="basic_select" items={basic_items} on-change={on_basic_change} />
            </div>

            <div class="demo-section">
                <h3>2. placeholder + cleanable</h3>
                <p>placeholder 设置占位文本，cleanable 启用清除按钮（默认关闭）。</p>
                <Select ref="cleanable_select" items={city_items} placeholder="请选择城市" cleanable on-change={on_city_change} />
                <p>选中城市：{selected_city}</p>
            </div>

            <div class="demo-section">
                <h3>3. appearance=false + menu_width/menu_max_h</h3>
                <p>appearance 默认 true，设为 false 移除边框与背景。menu_width/menu_max_h 控制下拉菜单宽度与最大高度。</p>
                <Select ref="minimal_select" items={lang_items} appearance="false" menu-width="240px" menu-max-h="200px" on-change={on_lang_change} />
                <p>选中语言：{selected_lang}</p>
            </div>

            <div class="demo-section">
                <h3>4. 尺寸 size</h3>
                <p>size 属性走通用 Sizable setter：xsmall / small / medium / large。点击按钮循环切换尺寸。</p>
                <p>当前尺寸：{size_label}</p>
                <div class="button-row">
                    <Button label="切换尺寸" on-click={on_cycle_size} />
                    <Select ref="sized_select" items={basic_items} size={size_value} />
                </div>
            </div>

            <div class="demo-section">
                <h3>API</h3>
                <Table columns={api_columns} rows={api_rows} bordered />
            </div>
        </template>
    </CaseDocPage>
</component>
