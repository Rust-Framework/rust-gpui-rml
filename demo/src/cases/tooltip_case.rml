<component>
    <CaseDocPage
        title={t("case.tooltip.title")}
        description="Tooltip 在 RML 中作为通用属性使用，而非独立组件。tooltip=\"text\" 生成 .tooltip(\"text\")，由专属 compiler/tooltip 模块处理组件白名单检查（Button / IconButton / DropdownButton / Toggle / Checkbox / Clipboard / Radio / Switch）。鼠标悬浮或聚焦时显示补充信息，常用于操作说明、快捷键提示。"
        code-rml={rml_sample}
        code-rust={rust_sample}>
        <template slot="demo">
            <div class="demo-section">
                <h3>基础用法（静态文本）</h3>
                <p>tooltip 属性接受字符串字面量，悬浮按钮查看提示。这是最常用的形式。</p>
                <div class="button-row">
                    <Button label="保存" tooltip="保存文件 (Cmd+S)" />
                    <Button label="删除" tooltip="删除选中项（不可恢复）" />
                    <Button label="撤销" tooltip="撤销上一步操作 (Cmd+Z)" />
                </div>
            </div>

            <div class="demo-section">
                <h3>动态绑定</h3>
                <p>tooltip={dynamic_tooltip} 绑定到 computed 方法，可动态切换提示文本。点击按钮循环切换 tooltip 内容。</p>
                <div class="button-row">
                    <Button label="切换 Tooltip" on-click={on_cycle_tooltip} primary="" />
                    <Button label="悬浮查看" tooltip={dynamic_tooltip} />
                </div>
                <p>当前 tooltip 内容：{dynamic_tooltip}</p>
            </div>

            <div class="demo-section">
                <h3>不同 variant 按钮的 tooltip</h3>
                <p>所有 Button variant 均支持 tooltip 属性。</p>
                <div class="button-row">
                    <Button label="Primary" primary="" tooltip="主要操作" />
                    <Button label="Danger" danger="" tooltip="危险操作" />
                    <Button label="Ghost" ghost="" tooltip="幽灵按钮" />
                </div>
            </div>

            <div class="demo-section">
                <h3>其他组件的 tooltip</h3>
                <p>Checkbox / Switch / Radio 等组件也支持 tooltip 属性（见白名单列表）。</p>
                <div display="flex" flex-direction="column" gap="8px">
                    <Checkbox tooltip="勾选即接受服务条款" label="同意条款" />
                    <Switch tooltip="切换自动保存模式" />
                </div>
            </div>
        </template>

        <template slot="api">
            <h3>Tooltip</h3>
            <Table columns={api_columns} rows={api_rows} bordered="" stripe="" />
            <p>另：tooltip 在 RML 中是通用属性，映射到 .tooltip(text)。仅支持白名单组件：Button / IconButton / DropdownButton / Toggle / Checkbox / Clipboard / Radio / Switch。</p>
            <p>另：tooltip 属性为字符串（静态）或 SharedString（绑定），不支持自定义内容元素。</p>
        </template>
    </CaseDocPage>
</component>
