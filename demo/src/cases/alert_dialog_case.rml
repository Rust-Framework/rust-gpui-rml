<component>
    <CaseDocPage
        title={t("case.alert_dialog.title")}
        description="AlertDialog 是基于 Dialog 封装的警示对话框组件，标签为 PascalCase AlertDialog。与 Dialog 的区别：默认 close-button=false + overlay-closable=false（强制用户交互），提供 description/confirm/show-cancel 便捷方法，footer 按钮居中对齐。适用于删除确认、不可逆操作等警示场景。受控模式（open 属性绑定字段）通过 ViewModel 状态驱动显示/隐藏。on-ok/on-cancel 回调返回 bool，返回 false 可阻止关闭对话框。"
        code-rml={rml_sample}
        code-rust={rust_sample}>
        <template slot="demo">
            <div class="demo-section">
                <h3>基础用法</h3>
                <p>AlertDialog 包裹 title 和 description 属性，提供简洁的警示信息展示。默认仅显示 OK 按钮，无关闭按钮、无遮罩点击关闭，强制用户点击确认。通过 Button on-click 打开，open 属性绑定 ViewModel 字段控制显示。</p>
                <Button label="显示提示" on-click={open_alert1} />
                <AlertDialog open={show_alert1} title="操作成功" description="您的更改已保存。" />
            </div>

            <div class="demo-section">
                <h3>确认对话框 confirm</h3>
                <p>添加 confirm 属性显示取消按钮，适用于需要用户确认的不可逆操作。confirm 属性等同于 show-cancel=true，同时显示 OK 和 Cancel 按钮。</p>
                <Button label="删除项目" danger on-click={open_alert2} />
                <AlertDialog open={show_alert2} title="确认删除" description="此操作不可撤销，确定要删除吗？" confirm />
            </div>

            <div class="demo-section">
                <h3>宽度控制 width</h3>
                <p>width 属性控制对话框宽度，支持 px 和裸数字单位。默认宽度为 420px。下方演示 width=500px 的效果。</p>
                <Button label="打开 500px 对话框" on-click={open_alert3} />
                <AlertDialog open={show_alert3} title="自定义宽度" description="此对话框宽度为 500px" width="500px" confirm />
            </div>

            <div class="demo-section">
                <h3>交互控制</h3>
                <p>AlertDialog 默认 close-button=false 和 overlay-closable=false。可通过 close-button=true 显示关闭按钮，overlay-closable=true 允许点击遮罩关闭，keyboard=false 禁用 ESC 键。</p>
                <div class="dialog-row">
                    <Button label="可关闭对话框" on-click={open_alert4} />
                    <AlertDialog open={show_alert4} title="可关闭" description="带关闭按钮和遮罩点击关闭" confirm close-button="true" overlay-closable="true" />
                    <Button label="禁用 ESC 对话框" on-click={open_alert5} />
                    <AlertDialog open={show_alert5} title="禁用键盘" description="ESC 键无法关闭此对话框" confirm keyboard="false" />
                </div>
            </div>

            <div class="demo-section">
                <h3>on-ok / on-cancel 事件回调</h3>
                <p>on-ok 和 on-cancel 回调返回 bool 值：返回 true 关闭对话框，返回 false 阻止关闭。下方演示事件回调场景。</p>
                <Button label="事件回调演示" on-click={open_alert6} />
                <AlertDialog open={show_alert6} title="确认操作" description="点击 OK 或 Cancel 关闭对话框" confirm
                    on-ok={on_alert_ok} on-cancel={on_alert_cancel} />
            </div>

            <div class="demo-section">
                <h3>与 Dialog 对比</h3>
                <p>AlertDialog 适用于简洁的警示确认场景（title + description + 确认/取消按钮），Dialog 适用于复杂内容场景（表单、设置面板等）。两者均通过受控模式（open 属性）触发。</p>
                <div class="dialog-row">
                    <Button label="AlertDialog" on-click={open_alert7} />
                    <AlertDialog open={show_alert7} title="AlertDialog" description="简洁警示，按钮居中" confirm />
                    <Button label="Dialog" on-click={open_alert8} />
                    <Dialog open={show_alert8} title="Dialog">
                        <div class="dialog-tip">复杂内容，按钮右对齐</div>
                    </Dialog>
                </div>
            </div>
        </template>

        <template slot="api">
            <h3>AlertDialog</h3>
            <Table columns={api_columns} rows={api_rows} bordered="" stripe="" />
            <p>注意：AlertDialog 默认 close-button=false + overlay-closable=false（与 Dialog 相反），适用于警示场景。</p>
            <p>另：confirm 属性等同于 show-cancel=true，显示 OK + Cancel 两个按钮（默认仅 OK）。</p>
            <p>另：on-ok/on-cancel 回调返回 bool，返回 false 可阻止关闭对话框。</p>
            <p>另：受控模式 <code>open=字段</code> 通过 ViewModel bool 字段驱动显示/隐藏，框架自动注入 on-close 回写 field=false。</p>
        </template>
    </CaseDocPage>
</component>
