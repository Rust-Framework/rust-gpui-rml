<component>
    <CaseDocPage
        title={t("case.tree.title")}
        description="Tree 是 RML 的树视图组件，标签为 PascalCase Tree。通过 ref 指令引用 TreeState，items 属性绑定 Vec<TreeData> 数据。on-activate 仅叶子节点触发，on-select 含文件夹节点触发。TreeData 通过 builder 模式构建（new/child/children/expanded/disabled），is_folder 根据 children 自动判断。框架在构造时自动将 TreeData 转换为内部 TreeItem。"
        code-rml={rml_sample}
        code-rust={rust_sample}>
        <template slot="demo">
            <div class="demo-section">
                <h3>1. 基础用法 + 事件对比</h3>
                <p>Tree 通过 ref 引用 TreeState，items 绑定 ViewModel 中的 Vec&lt;TreeData&gt; 字段。Tree 标签同时绑定 on-activate 和 on-select 两个事件：on-activate 仅叶子节点点击触发，on-select 含文件夹节点触发。点击文件夹节点仅展开/折叠（不触发 on-activate），点击叶子节点同时触发两个事件。</p>
                <p>当前激活（叶子）：{activated_text}</p>
                <p>当前选中（含文件夹）：{selected_text}</p>
                <Tree ref="basic_tree" items={tree_items} on-activate={on_activate} on-select={on_select} />
            </div>

            <div class="demo-section">
                <h3>2. TreeData 构建模式</h3>
                <p>TreeData 通过 builder 模式构建：TreeData::new(id, label) 创建根节点，.child(...) 添加单个子节点，.children(iter) 添加多个子节点。.expanded(true) 设置初始展开状态，.disabled(true) 禁用节点（禁用节点不触发事件）。is_folder() 根据 children 是否为空自动判断，无需手动设置。</p>
                <p class="hint">TreeData::new("group1", "分组一").expanded(true).child(TreeData::new("item1", "选项 1")).child(TreeData::new("item2", "选项 2")) —— 文件夹节点，expanded(true) 初始展开。TreeData::new("group2", "分组二").child(...) —— 文件夹节点，默认折叠。TreeData::new("item5", "独立项") —— 叶子节点（无 children）。TreeData::new("disabled_item", "禁用项").disabled(true) —— 禁用节点，点击无事件触发。</p>
            </div>

            <div class="demo-section">
                <h3>3. 多层级嵌套</h3>
                <p>TreeData 支持任意深度嵌套：.child() 可链式调用，子节点也可继续 .child() 添加孙节点。depth 由 TreeState 内部计算（渲染时缩进 = 16px * depth），无需手动设置。is_folder() 仅判断直接 children，孙节点不影响父节点的 folder 判断。</p>
                <p class="hint">示例：TreeData::new("root", "根").child(TreeData::new("child", "子").child(TreeData::new("grandchild", "孙"))) —— 三层嵌套，root 和 child 是文件夹，grandchild 是叶子。</p>
            </div>
        </template>

        <template slot="api">
            <h3>Tree</h3>
            <Table columns={api_columns} rows={api_rows} bordered="" stripe="" />
            <p>另：Tree 通过 ref + items 声明式绑定。TreeData 是 ViewModel 侧数据载体（Send + Sync），框架在构造时自动转换为内部 TreeItem。</p>
        </template>
    </CaseDocPage>
</component>
